# Stories 3.1–3.2 — revisão especializada e preparação do push

Data: 18/09/2026 (America/Sao_Paulo). Base Git: `020bcc525e97170cd7583742adb2c63b95ee5a0b`.

Revisão local executada pelo mesmo agente Codex, usando sequencialmente as skills de dados (Dara), arquitetura (Aria), UX (Uma), desenvolvimento (Dex), QA (Quinn) e DevOps (Gage). Não representa revisão independente por pessoas diferentes nem homologação de produção.

## Escopo e decisões

Stories 3.1 e 3.2 estão integradas no mesmo worktree: início idempotente da visita, preservação do contexto da rota, estoque observado offline, sincronização e revogação entre abas. O commit conjunto preserva essa dependência. Não altera AB-01, regras comerciais, dados reais, dependências, infraestrutura remota, n8n ou critérios de piloto.

| Revisão | Resultado no recorte local | Evidência principal |
| --- | --- | --- |
| Banco — Stories 3.1/3.2 | PASS após correções | ACL/roles/RLS/constraints, migrações incrementais, pgTAP, integração e preflight de rollback |
| Arquitetura/segurança — Stories 3.1/3.2 | PASS | Contratos/camadas, autorização atual, contexto histórico, atomicidade e variante RPC documentada |
| UX — fluxo das duas stories | PASS no navegador testado | Teclado/foco, mensagens, tamanhos dos controles, contraste e reflow de 320 px |
| QA 3.1 | CONCERNS mantido | SEC-001 resolvida; três recomendações low anteriores permanecem |
| QA 3.2 | PASS mantido | AC 1–9 preservados, com novas regressões para os achados abaixo |

## Banco: achados e correções

1. **DB-ACL-01 — privilégios implícitos dos helpers de estoque.** A inspeção de `pg_proc`/`aclexplode` encontrou `EXECUTE` de PUBLIC em `private.get_visit_stock_result` e `private.apply_visit_stock_saved`, sem grant nominal ao executor de sincronização. `anon`/`authenticated` já estavam sem `USAGE` em `private`: não foi demonstrado vazamento público, mas o privilégio mínimo dependia de um grant implícito inadequado. A migração `20260918170000_visit_stock_contract_acl.sql` revoga PUBLIC/anon/authenticated no contexto do owner e concede apenas `cirne_sync_executor`. Seis asserções verificam grants positivos/negativos.
2. **DB-CONTRACT-01 — versão do comando direto.** A RPC direta convertia o comando de estoque para evento v1 sem rejeitar todas as versões inválidas. A mesma migração passa a exigir JSON numérico `schemaVersion: 1`. Testes cobrem ausência, versão não suportada e string `"1"`; a fronteira PostgreSQL não depende apenas da validação do BFF.
3. **DB-REPLAY-01 — resposta antiga após edição nova.** O replay direto de uma intenção previamente confirmada retornava as quantidades do snapshot mais recente. A migração `20260918173000_visit_stock_replay_result.sql` mantém a autorização e a comparação integral do hash em `private.sync_event`, mas devolve as quantidades/observação daquela intenção e seu `confirmed_at` persistido. O ID canônico permanece estável; a edição mais recente não é sobrescrita. Duas regressões provam igualdade da resposta original e preservação do snapshot atual.

Prova antes/depois: sete asserções falharam antes do primeiro ajuste (quatro de ACL e três de versão); o replay também foi reproduzido antes de sua correção. A suíte de estoque passou de 34 para 45 asserções. Total pgTAP: **345**, em sete arquivos.

### Migrações, segurança e recuperação

- Migrações novas aplicadas somente ao Supabase local pela CLI versionada. Cada uma foi ensaiada com `ROLLBACK` antes do `COMMIT` real; nenhuma migração anterior foi reescrita para estes ajustes.
- Transação, `lock_timeout = '5s'` e advisory lock de migração nas duas novas migrações. Nenhum backfill, drop de dados ou nova extensão.
- Snapshots locais de schema/ACL antes e depois em `.local/review-3.2/`: `before-contract-acl.sql`, `after-contract-acl.sql` e `after-replay.sql`. São ignorados pelo Git; não são backup completo de Auth, Storage ou dados.
- No primeiro ajuste, contagens antes/depois foram iguais: 77 visitas, 8 snapshots de estoque, 117 confirmações e 357 eventos de auditoria. Suítes posteriores acrescentam seus fatos sintéticos normalmente; não houve reset/limpeza de histórico.
- Tabelas da capability em `api` têm RLS e FORCE RLS; executores são NOLOGIN/NOSUPERUSER/NOBYPASSRLS. Usuários finais não possuem DML direto; funções derivam identidade do JWT e revalidam situação, capacidade e ownership, inclusive no replay.
- `private.sync_events`/`private.audit_events` não dependem de RLS para a barreira pública: schema e tabelas não são acessíveis a `anon`/`authenticated`. Não se declara que todas as tabelas privadas possuem RLS.
- PK/FK/UNIQUE/CHECK, relacionamento visita/parada/cliente, snapshot 1:1 e índices de acesso foram inspecionados. Planos locais utilizaram índices; volumes sintéticos pequenos não certificam desempenho de campo.
- Rollback de estoque foi inspecionado e somente seu prefixo seguro executado: recusou com `Rollback refused: stock snapshots exist`, antes de qualquer `DROP`. O rollback de início também é protegido contra histórico e tem teste de integração. O corpo destrutivo não foi executado neste banco com fatos.
- Os checklists DBA de predeploy/rollback foram aplicados ao escopo local. Backup completo/restauração, janela e operador de produção permanecem gates de uma implantação futura. O runner registra versões; isso não significa que o SQL bruto de criação inicial possa ser repetido indiscriminadamente.

## Arquitetura e segurança

- CLI/API/PWA usam contratos compartilhados; domínio permanece independente de React, Next.js, Supabase e Dexie.
- BFF valida autenticação, capacidade, CSRF no fluxo por cookie, tamanho do corpo e respostas. Usa o JWT do chamador; não introduz credencial administrativa no fluxo normal. Erros não revelam recursos de outro vendedor nem detalhes internos.
- Visita/outbox e estoque/outbox são gravados atomicamente no IndexedDB; fato, idempotência e auditoria são atômicos no PostgreSQL. Confirmação é individual por evento. Ordem, retries com a mesma chave, perda de resposta, confirmação parcial e duas sessões reais têm cobertura existente.
- Contexto da rota publicada e parâmetros vinculados continuam imutáveis; autorização é atual e não derivada do snapshot. Visita antiga de cliente retirado é preservada conforme a decisão já registrada na Story 3.1.
- Revogação compartilhada oculta rota, shell offline e visita, invalida respostas tardias e preserva trabalho pendente. Migrações Dexie v1–v7 são aditivas; atualização não recria o banco nem apaga a outbox.
- A variante implantada de wrappers `api.* SECURITY DEFINER`, schema privado sem USAGE para usuários e executores donos de suas tabelas foi explicitada em `Docs/architecture.md` §9.12. FORCE RLS não substitui a autorização explícita das funções; a revisão não presume herança da RLS do chamador.
- Persistência local não é criptografia nem proteção contra acesso ao perfil do navegador. Não há nova promessa de conformidade LGPD, retenção ou privacidade em aparelho físico.

## UX e acessibilidade

- Formulário preserva zero, rejeita vazio/negativo/decimal e mantém valores quando o commit falha. Sucesso só aparece depois da gravação local; sincronização é um estado separado.
- Foco inicial no título da etapa; após erro, no primeiro campo inválido; após salvar, no título de confirmação; ao editar, no título do formulário. Labels e mensagens usam associação semântica.
- Novo E2E percorre o fluxo com Tab/Enter, exige foco correto e alvos mínimos de 44 × 44 px, salva/reabre valores e verifica 320 px sem rolagem horizontal.
- A inspeção das primeiras capturas revelou que “Sincronizada” ultrapassava o cartão em 320 px, embora a página não tivesse overflow. Tipografia responsiva e quebra segura foram adicionadas, com asserção do `scrollWidth` do próprio status.
- Borda dos campos reforçada para `#738779` sobre `#fbfcfb` (contraste 3,73:1). Progresso passa a usar `--cirne-green-text`, evitando o verde decorativo em texto pequeno. Demais pares conferidos: texto secundário/branco 5,69:1; erro/branco 8,67:1; branco/ação primária 5,60:1; foco/branco 5,14:1.
- Capturas sintéticas são artefatos locais ignorados pelo Git. A imagem final `stock-mobile-320.png` foi inspecionada: status dentro do cartão, campos e botões sem cortes, foco visível e progresso legível. Artefatos em `.local/review-3.2/browser-final/`; capturas de erros também conferidas.
- Android físico, TalkBack, zoom real de 200%, instalação/encerramento da PWA, quota e p95 permanecem pendentes antes do piloto. Não se declara certificação WCAG global.

## Regressão e rastreabilidade

Snapshot de produto: `product-files-sha256:b0a5b08ee580a1cc4cbdcf29d7d1c7b591a714cb780cc660cea60ef9c591e626` (185 arquivos). Algoritmo: união sem duplicatas de `git ls-files -c -o --exclude-standard -z`, ordenada lexicograficamente; inclui `apps/`, `packages/`, `scripts/`, `supabase/`, `tests/`, `package.json`, `package-lock.json`, `tsconfig*.json` e configurações raiz `*.config.ts|mjs`. SHA-256 recebe, por arquivo, caminho POSIX + NUL + bytes + NUL. Documentos não entram no digest; Git identifica a revisão publicada.

As execuções após as correções de banco e foco aprovaram 204 unitários, 345 pgTAP, 16 integrações, 42 E2E controlados e 4 reais: **611 testes distintos**. pgTAP não é contado duas vezes por também integrar o comando `test:integration`. Uma reexecução isolada do teste de teclado não aumenta esse total.

A passagem CodeRabbit uncommitted completa, incluindo arquivos novos e as duas migrações corretivas, terminou com zero findings. A revisão committed contra `master` do commit `dec7e7a8c36b38ac812941ef35dcbb205b3e8687`, incluindo o ajuste visual final de tipografia/contraste e sua asserção, também concluiu com `review_completed`, **0 findings**, nos 70 arquivos da mudança. Uma tentativa anterior, feita antes da criação do commit, retornou `review_skipped`; ela não foi usada como aprovação.

Pendências não bloqueantes preservadas: TEST-001, MNT-001 e TEST-002 da Story 3.1; ensaio explícito de contenção entre PUT direto e sync-batch de estoque recomendado na Story 3.2. Nenhuma dessas recomendações foi marcada resolvida sem prova.

### Fechamento DevOps

- Build, lint e 204 unitários reexecutados após o último ajuste CSS: PASS.
- Typecheck e regressão completa de navegador do snapshot final: PASS — 42 controlados e 4 reais, após o ajuste de tipografia/contraste. Banco/integração: 345 pgTAP e 16 integrações após a correção final de replay. Total: 611 testes.
- `npm audit --audit-level=high`: zero vulnerabilidades.
- Port denylist canônica: 1.126 arquivos, zero ocorrências. O projeto não tem o alias npm do framework; foi chamado diretamente o módulo `port-denylist.js`, sem editar o framework.
- Secretlint (`npx --yes @secretlint/quick-start --no-glob` com os arquivos alterados/novos): PASS, nenhum segredo detectado. Ferramenta temporária, sem alteração de dependências/lockfile do projeto.
- CodeRabbit committed: PASS, 70 arquivos, zero findings. Nenhum CRITICAL/HIGH pendente.
- Git: `origin/master` atualizado e ancestral da branch; nenhum conflito; File Lists cobrem todos os 70 arquivos, sem caminhos ausentes ou duplicados; diff-check completo aprovado. O commit de produto estava com árvore limpa. Este fechamento altera somente a evidência e os checkboxes pré-PR, em commit documental separado.
- Escopo: sem dependências novas, alteração de versão, tag ou release. Recomendações low não bloqueantes explicitadas no PR; testes de cenários não são declaração de cobertura percentual nem ausência universal de flakiness.
- Autorização: usuário solicitou `@devops *push` e reiterou “prossiga o maximo que puder”. Publicação da branch e abertura de PR, sem force push. Nenhum merge, deploy, release ou uso de dados reais é autorizado por este relatório.

### Acompanhamento do PR e correção do teste no CI

Push inicial concluído e [PR #4](https://github.com/cirnebaterias-web/crinerotas/pull/4) aberto. As execuções [pull_request](https://github.com/cirnebaterias-web/crinerotas/actions/runs/35410054998) e [push](https://github.com/cirnebaterias-web/crinerotas/actions/runs/35410052223) falharam no mesmo teste de perda da resposta do estoque, com 41 dos 42 E2E aprovados. Lint, typecheck, unitários e build remotos passaram.

A causa era a espera do teste: imediatamente após o clique, a outbox podia ainda estar vazia porque o commit local não tinha terminado. O polling de fila vazia aprovava esse estado anterior e consultava o contador de envios ainda `null`, em vez de observar as duas tentativas. Não era evidência de um replay confirmado incorreto nem motivo para relaxar as asserções.

`tests/e2e/visit-start.spec.ts` agora aguarda a confirmação visual do commit, observa exatamente dois envios e só depois exige outbox vazia, igualdade integral do lote e snapshot sincronizado. Sem sleep fixo, retry de teste, alteração de aplicação/banco ou aumento de timeout. O cenário passou dez vezes consecutivas; lint, typecheck, 204 unitários, Secretlint e CodeRabbit uncommitted do ajuste passaram (zero findings). As repetições não aumentam o total de 611 testes distintos.

Snapshot após o ajuste de teste: `product-files-sha256:840d2ac5b1390d0b6fae2426cb18d9a2834e8c464aab16ad92cddf19b1408135`, pelo mesmo método de 185 arquivos descrito acima. A evidência de build/pgTAP/integração/E2E real anterior permanece aplicável, pois não houve alteração nos arquivos de aplicação, banco, configuração ou dependências.

Reexecução completa após o ajuste: **42/42 E2E controlados aprovados**, além das dez repetições isoladas. QA mantém CONCERNS para 3.1 e PASS para 3.2; revisão automatizada incremental sem achados e critérios de idempotência preservados. A nova execução remota ainda precisa passar antes de integrar o PR.

Na rodada seguinte, o [CI do PR em 8358b1a](https://github.com/cirnebaterias-web/crinerotas/actions/runs/35410708503) passou integralmente, inclusive Supabase limpo, integração, imagem Docker, usuário não-root e HTTP canary. O [CI paralelo do push](https://github.com/cirnebaterias-web/crinerotas/actions/runs/35410705933) passou pelo retry corrigido, mas expôs outra corrida na preparação do cenário `offline-shell/session-channel`.

O emissor enviava o sinal após `domcontentloaded`, antes de terminar sua própria validação de sessão. A revogação durante `requireLocalAccess` deve invalidar uma gravação de sessão tardia, o que pode legitimamente remover o marcador de vendedor; o teste esperava o estado de uma sessão já carregada. Agora a segunda aba precisa exibir as visitas históricas antes do disparo. As asserções de ocultação, preservação integral de draft/outbox e isolamento de identidade não mudaram. Os testes de serviço que bloqueiam a gravação e revogam acesso durante `initialize`/`refresh` continuam intactos.

O segundo ajuste também ficou limitado ao E2E: dez repetições do cenário aprovadas, lint/typecheck/204 unitários aprovados e CodeRabbit uncommitted sem achados. Snapshot complementar: `product-files-sha256:c3e657d08d112f73067f50bd427c29b6159f1d84b7b806dd0531c1cf617cb617`.

Suíte controlada completa após o segundo ajuste: **42/42 aprovados**. O total distinto permanece 611, com vinte repetições adicionais dos dois cenários, não somadas à contagem. Secretlint/diff-check aprovados. Nenhum artefato de aplicação/banco mudou desde a execução Linux completa aprovada; os checks do novo commit serão acompanhados no PR antes de qualquer integração.

### Revisão remota do CodeRabbit antes do merge — 18/09/2026

O CI de `69b6c10` passou nas duas execuções. Depois, a revisão GitHub do CodeRabbit terminou em 19/09/2026 01:22 UTC e trouxe quatro comentários acionáveis, um documental fora do diff e um nitpick. O status verde do bot não foi interpretado como ausência de achados. As observações foram verificadas no código antes de qualquer merge:

- **Rascunho legado duplicado (comentário 4051801797): confirmado.** Dois testes falharam antes da correção, para rascunho ainda na outbox e já confirmado. `saveVisitStartAndEnqueue` agora reutiliza `offlineId`, preserva os campos existentes e calcula a próxima sequência pelo maior valor pendente/confirmado; a atualização e o novo evento permanecem atômicos. Um terceiro teste prova a preservação do rascunho/evento antigo quando a inclusão na outbox falha.
- **ACL da migração inicial (4051801806): confirmado.** O teste executa rollback/reaplicação em banco temporário vazio com o runner `postgres` não-superuser. A migração original terminava, mas a verificação dos grants retornava falso. `SET LOCAL ROLE` passa a envolver separadamente os blocos dos owners, antes de revogar memberships temporários. Corrigido o SQL inicial ainda no PR não integrado; as migrações corretivas incrementais permanecem intactas para bancos que já o aplicaram. Não houve reaplicação da migração inicial no banco de trabalho.
- **Permissão de rename no rollback (4051801823): confirmado.** O ensaio isolado reproduziu `permission denied for schema private`. CREATE é concedido temporariamente ao executor e revogado após o rename, mantendo preflights e restauração dos memberships.
- **Windows/Rancher obrigatório (4051801833): corrigido.** Os testes de integração usam `docker` direto por padrão. O launcher Rancher/WSL só é selecionado no Windows com `CIRNE_DOCKER_BACKEND=rancher-desktop-wsl`; o escape de `$` em argumentos limita-se a esse launcher. Nenhuma instalação/runtime foi alterada. A revisão incremental detectou a falta dessa opção no novo teste de lifecycle; o helper foi alinhado antes da publicação.
- **Documento histórico e diagnóstico de fixture:** a arquitetura distingue a revisão documental original das capacidades já implementadas; o erro da fixture preserva `cause` sem alterar sua mensagem contextual.

`visit-stock-lifecycle.test.ts` captura schema-only de auth/api/private em memória, restaura em banco de nome aleatório validado, verifica ausência de estoque, executa rollback/reaplicação como não-superuser e confere grants positivos/negativos e remoção de CREATE. Remove somente esse banco temporário em `finally`; nenhum dado é copiado, apagado ou revertido no banco de trabalho. O teste passou após reproduzir separadamente as duas falhas de permissões. O clone não é backup de produção.

O aviso de cobertura de docstrings do perfil padrão do bot é consultivo: não é um novo requisito das stories e não justifica expandir este ajuste para documentação automática de todas as funções.

Uma primeira rodada local sobrepôs por engano build e integração: a limpeza do standalone encontrou EBUSY e o teste de configuração encontrou `server.js` ausente. Não foi tratado como regressão do produto nem suprimido; build e suítes foram reexecutados sequencialmente.

Revalidação local concluída: lint/typecheck/build/diff-check PASS; **207 unitários, 345 pgTAP, 17 integrações, 42 E2E controlados e 4 reais = 615 testes distintos**. O lifecycle foi adicionalmente aprovado tanto com Docker direto quanto com a opção Rancher/WSL, sem somar repetições. Secretlint sem achados; `npm audit --audit-level=high` sem vulnerabilidades. CodeRabbit incremental final: `review_completed`, zero findings, incluindo o helper corrigido e o novo teste.

Quinn/Codex revalidou o delta contra AC 2/5/6/9 da Story 3.1 e AC 5/6/9 da Story 3.2. Mantidos CONCERNS (três recomendações low anteriores) e PASS, respectivamente, sem nova transição de ciclo de vida nem aprovação de piloto. Snapshot atual: `product-files-sha256:0e2aa06935adad03befbae6dcee44a13ac8668223c38d59796f2711f6710af42` (186 arquivos, mesmo algoritmo). File Lists cobrem todo o delta, inclusive o novo teste; nenhum caminho listado ausente. Sem alteração de dependências/lockfile, contrato comercial, dados reais ou infraestrutura remota. A revisão GitHub e o CI do novo commit ainda precisam concluir antes do merge.
