# CodeRabbit — Story 3.2

Data: 2026-09-18. Escopo: diff local não commitado, incluindo arquivos rastreados e não rastreados das Stories 3.1/3.2. Comando disponível na versão instalada: `coderabbit review --uncommitted --include-untracked --agent --light`.

## Rodada 1

Status `review_completed`, 4 achados:

1. **Major — ACL de `private.sync_event`:** confirmado. O privilégio padrão de `PUBLIC` permitia que funções security-definer dependessem de acesso implícito. Duas migrações aditivas revogam `PUBLIC`/`anon`/`authenticated` e concedem `EXECUTE` explicitamente a `cirne_visit_executor`. pgTAP confirma ambos os lados; 330 asserções passaram.
2. **Major — ownership/RLS no rollback:** confirmado. O rollback agora concede `SET ROLE` temporário, trava/verifica snapshots como `cirne_visit_executor`, trava/verifica eventos e altera objetos como `cirne_sync_executor`, e executa cada DDL como o proprietário correto.
3. **Minor — inventário de API no README:** corrigido com `PUT /api/v1/visits/{offlineId}/sections/stock`.
4. **Minor — progresso antes do commit:** corrigido; “ETAPA 1 DE 4” fica oculto enquanto não existe `draft.stock`. Playwright cobre a condição.

## Rodada 2

Status `review_completed`, 3 achados major relacionados à preservação de memberships temporárias.

- O rollback, ainda não aplicado, passou a registrar a existência e o `set_option` de grants feitos pelo mesmo grantor, revogando apenas memberships criadas pelo script e restaurando `SET FALSE` quando necessário.
- As migrações `20260918160000` e `20260918161000` já haviam sido aplicadas antes da rodada e não foram reescritas, conforme a regra expand-only da story. Ambas usam `GRANTED BY current_user`, portanto a revogação é limitada ao grant daquele grantor. Inspeção em `pg_auth_members` confirmou que as memberships preexistentes de `postgres` pertencem a `supabase_admin`, com `set_option=false`, e continuaram idênticas após aplicação e após o preflight abortado.
- Esse alerta fica documentado como hipótese defensiva para um executor de migração diferente do ambiente suportado, não como corrupção observada. Não há finding critical. A configuração light da story exige correção automática de critical e documentação de high/major; o critério foi atendido em duas iterações.

## Verificação posterior

Lint, tipos, 203 unitários, build, 330 pgTAP, 16 integrações, 41 E2E controlados e 4 E2E reais passaram. O preflight do rollback recusou histórico sob o papel proprietário sem executar DDL destrutivo. Nenhum segredo, observação ou payload privado foi incluído neste relatório.
