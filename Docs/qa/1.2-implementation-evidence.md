# Story 1.2 — Evidência de implementação

Data: 2026-09-11  
Escopo: identidade local, perfis, papéis, capacidades, escopos, bloqueio, RLS, `GET /api/v1/me` e CLI segura.

## Resultado

Implementação pronta para revisão formal. A base limpa aplica a migração versionada, provisiona cinco atores exclusivamente sintéticos e preserva o isolamento esperado. Nenhum remoto, Supabase Cloud, VPS, push ou deploy foi criado.

## Evidências reproduzíveis

| Verificação | Resultado |
| --- | --- |
| `npm run db:reset` | PASS; migração confirmada, rede dedicada restaurada e atores provisionados |
| Repetição de `npm run identity:provision -- --json` | PASS; mesmos IDs, sem duplicação ou credencial na saída |
| `npm run db:test` | PASS; 1 arquivo, 38 assertions pgTAP |
| `npm run lint` | PASS; zero erros/avisos |
| `npm run typecheck` | PASS; tipos do Next e TypeScript estrito |
| `npm test` | PASS; 11 arquivos, 35 testes unitários |
| `npm run build` | PASS; rotas `/`, `/api/v1/health/live` e `/api/v1/me` |
| `npm run test:integration` | PASS; 1 arquivo, 7 testes com servidor e Supabase reais |
| `npm audit --omit=dev --json` | PASS; zero vulnerabilidades conhecidas nas dependências de produção |
| `git diff --check` | PASS |

Os testes confirmam grants e RLS, função interna com owner técnico `NOLOGIN`/`NOBYPASSRLS`, `search_path` vazio, negação de `anon`, perfil bloqueado, isolamento entre vendedores, escopo do gestor somente sobre Vendedor A, contrato e correspondência do usuário autenticado. A integração comprova `401`, `403`, conflito de credenciais, sucesso de vendedor/gestor e execução real da CLI sem token em argumentos, stdout ou stderr.

## Segurança e dados locais

- O schema exposto é `api`; `private` não é exposto e `public` não recebe tabelas de domínio.
- A rota e a CLI usam apenas URL e chave publicável. Chave administrativa existe somente no adaptador local de provisionamento.
- O arquivo `.local/identity-actors.json` é ignorado pelo Git e armazena credenciais aleatórias em domínio `.invalid`; nenhuma credencial fixa ou dado pessoal entra na migração.
- `apps/web/.env.local` é atualizado apenas com valores públicos locais; o configurador rejeita origem remota e chaves `SECRET`/`SERVICE_ROLE`.
- O BFF valida o usuário com Supabase Auth e resolve papéis/capacidades/escopos vigentes no banco. Cookies PKCE não são tratados como sessão.
- Erros públicos e logs usam campos permitidos e não incluem token, cookie, e-mail, senha, corpo ou chave.

## Revisão CodeRabbit

Duas passagens light cobriram todos os arquivos alterados e não apontaram severidade crítica. A primeira retornou cinco achados válidos, corrigidos: destino remoto da CLI, falha de logging no `finally`, normalização da chave pública, conflito stdin/ambiente e grant remanescente no rollback. A segunda retornou um achado válido, corrigido: cookie PKCE confundido com sessão. Os gates completos foram repetidos após a última correção.

## Definition of Done

- [x] Requisitos funcionais e oito critérios de aceite implementados.
- [x] Estrutura, stack, contrato, modelo e regras de segurança do projeto respeitados.
- [x] Sem segredo hardcoded, novo erro de lint ou acesso administrativo genérico.
- [x] Testes unitários, pgTAP e integrações reais implementados e aprovados.
- [N/A] Meta numérica de cobertura não existe no projeto; caminhos positivos e negativos definidos na story estão cobertos.
- [x] Funcionamento verificado por reset, provisionamento repetido, build e servidor de produção local.
- [x] Casos ausente, inválido, conflitante, bloqueado, fora de escopo, drift de contrato e rede indisponível tratados.
- [x] Tasks, Change Log, Dev Agent Record, File List, README e evidências atualizados.
- [x] Dependências `@supabase/supabase-js` e `@supabase/ssr` previstas pelo adaptador Supabase, fixadas no lockfile e auditadas.
- [x] Novas variáveis e procedimentos locais documentados sem valores secretos.
- [N/A] Interface visual, E2E de navegador, remoto e deployment estão explicitamente fora do escopo.
- [x] Developer Agent confirma que todos os itens aplicáveis foram atendidos e a story está pronta para revisão.

## Limites preservados

`AB-15` continua aberto; portanto, somente dados sintéticos são permitidos. Revogação imediata de refresh tokens emitidos não foi ampliada além das capacidades locais do Auth, mas toda resolução BFF/RLS revalida o estado e nega o ator bloqueado. O rollback foi revisado estaticamente; a evidência operacional principal usa recriação limpa para frente. A promoção para VPS continua sendo um incremento futuro sujeito a configuração e autorização próprias.
